function dab_data = error_correct(dab_data_snapped)
    % ---------------------------------------------------------------------    
    % ERROR_CORRECT: Apply error correction algorithm to DAB data
    % ---------------------------------------------------------------------
    % NB: NOT IMPLEMENTED IN THIS PROJECT
    % ---------------------------------------------------------------------
    % Proposed Usage:
    %  Inputs
    %   > dab_data_snapped:     Snapped DQPSK data
    %                               [(dab_mode.L - 1) x dab_mode.K]
    %
    %  Outputs
    %   < dab_data:             Error-corrected DQPSK data
    %                               [(dab_mode.L - 1) x dab_mode.K]
    %
    % ---------------------------------------------------------------------    
    dab_data = dab_data_snapped;
end